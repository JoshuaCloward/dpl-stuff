<h1 class="page-header"> All Questions </h1>

<ul>
  <% @questions.each do |question| %>
  <li>
    <%= link_to question.name, question_path(question.id), class: "question"%>
    <%#= link_to 'Destroy', question_path(question.id), method: :delete, {confirm: 'are you sure?'} %>
  </li>
  <% end %>
</ul>
